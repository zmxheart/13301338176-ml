Example one shot learning detection of two digit numbers using deep CNN. More description is coming. 

To run the code, run `python main.py`. Requires Numpy, Keras and Theano or TensorFlow (untested with TF).
